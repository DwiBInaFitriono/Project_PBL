; Rebung Pintar — PHP configuration for Vercel serverless runtime
; Enables password hashing and core extensions needed for bcrypt

memory_limit = 256M
max_execution_time = 30

; Ensure hash extension is loaded (needed for password_hash / bcrypt)
extension = hash.so

; OpenSSL needed for bcrypt on some Amazon Linux PHP builds
extension = openssl.so
